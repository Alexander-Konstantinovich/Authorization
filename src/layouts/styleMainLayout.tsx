import styled from "styled-components";

export const DivLayout = styled.div<{ mode?: string }>`
  display: flex;
  width: 100vw;
`
export const DivLayoutOutlet = styled.div<{ mode?: string }>`
  margin-left: auto;
	margin-right: auto;
  width: 100%;
`