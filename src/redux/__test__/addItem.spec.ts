import tableReducer from "../table/slice"
import initialState from "../table/slice"
import addItem from "../table/slice"


// describe("reducer addItem", () => {
//   it("check addItem", () => {
//     const addItemState = tableReducer(initialState:TableInitial, addItem(),)

//     expect(initialState.items).toEqual([])
//   })
// })